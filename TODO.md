## TODO
1. add link to parent process in `.html` files, OK
2. fix colored output in `.html` files (see `./test/test-color.sh`), OK
3. get rid of special terminal symbols in `.html` files (see `./test/test-color.sh`), OK
4. replace `link` in nohup tests with full command, OK
5. take care of warnings during compilation, OK
6. add SSH, sudo and rsync handling (consult with the maintainer of the package on `AUR`)